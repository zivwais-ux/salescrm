# אפיון המערכת — BENY

## 1. הבעיה

בני מקבל מה-ERP דוח PDF בשם **"ניתוח מכירות (ת.משלוח) — מחירים"**: טבלה של
לקוח מול חודש, שמונה עמודים, מאות שורות, בלי סיכומים שימושיים ובלי יכולת
לשאול אותה שאלות. כדי לענות על "מי ירד השנה?", "מי לא קנה כבר חודשיים?",
"כמה נסגור בסוף השנה?" צריך לפתוח אקסל ולעבוד ידנית בכל פעם מחדש.

## 2. מה המערכת עושה

אפליקציית ווב אחת בעברית (RTL) שמחזיקה את אותם נתונים, ומוסיפה עליהם ניתוח,
גרפים, עריכה אינטראקטיבית ומעקב אחרי לקוחות. האקסל לא נעלם — הוא נשאר הפלט
שאפשר לשלוח הלאה, ומופק מהמערכת בלחיצה.

**אין שרת ואין חשבון להקים.** פותחים את הכתובת והמערכת עובדת, גם בלי
אינטרנט. הנתונים נשמרים בדפדפן; הניוד והגיבוי הם קובץ.

## 3. הנתונים

### מקור
חמישה דוחות PDF: **2022, 2023, 2024, 2025** (שנים מלאות) ו-**2026**
(ינואר–ספטמבר). `tools/parse_pdf.py` בונה מחדש את הטבלה מתוך מיקומי התווים
בעמוד — שורות לפי עמודת המטבע, עמודות לפי כותרות "מחיר כולל", וטקסט עברי לפי
סדר ימין-לשמאל. `tools/build_dataset.py` קורא את השנה מתוך שורת התקופה של
הדוח עצמו, ומשמר שנה שכבר יש במאגר ואין לה PDF — כך אפשר להוסיף דוח בודד
בלי שאר הקבצים.

**בקרת נכונות.** שורת "סה״כ כללי" שהדוח מדפיס בעצמו נשמרת בנפרד מהנתונים,
ומסך "נתונים וקבצים" משווה מולה חודש-חודש, בבורר שנה. כל 57 החודשים
והסכומים הכוללים תואמים לאגורה:

| שנה | מחזור בדוח |
|---|---|
| 2022 | ‎15,483,017.74 ₪ |
| 2023 | ‎18,441,363.69 ₪ |
| 2024 | ‎16,183,215.31 ₪ |
| 2025 | ‎16,203,057.74 ₪ |
| 2026 (ינואר–ספטמבר) | ‎11,228,824.09 ₪ |

גם כל סיכום סוכן שהדוחות מדפיסים תואם במדויק: ארבעה-עשר הסיכומים של
2022–2024, וארבעת הסיכומים של 2025 שנבדקו כשהדוח הזה נקרא. אחרי
עריכה ידנית הטבלה מראה בדיוק במה המערכת כבר שונה מהדוח, במקום להניח שהיא
זהה לו.

**שני דברים שהדוחות הישנים לימדו:**

- **שורות במטבע אחר.** שבע שורות (כ-0.6% מהמחזור) תומחרו ביורו. הדוח מחבר
  אותן לסיכומים שלו כמו שהן, וכך גם המערכת — אחרת ההצלבה היתה מראה פער
  קבוע. הן מסומנות במסך "נתונים וקבצים", והמטבע הוא חלק מזהות התנועה: בלעדיו
  שורת יורו ושורת שקל של אותו לקוח באותו חודש דורסות זו את זו.
- **חודש חלקי.** דוח שהופק באמצע החודש סופר בו רק חלק מהמשלוחים — ספטמבר
  2026 מראה 387 אלף מול כ-1.4 מיליון בחודש רגיל. הסכום נכון ונשאר כמו שהוא,
  אבל חודש כזה מזוהה אוטומטית (פחות ממחצית החציון של ששת החודשים שלפניו),
  מסומן במסך, ואינו נספר בממוצע החודשי, בתחזית ובחלון שנים-עשר החודשים.

### מבנה
| ישות | תיאור |
|---|---|
| סוכנים | עשרה בחמש השנים. הגדולים: בני ויסמן (36), שוקי צוקרון (510), אייל בן אבי (58), דני חיים (62) |
| לקוחות | כל גוף עסקי — אותו מספר יכול להופיע כלקוח מקבל וגם כלקוח משלם |
| פרופיל לקוח | מה שבני מנהל בעצמו: סטטוס, דירוג, איש קשר, הערות |
| מכירות | תנועה לכל לקוח × משלם × סוכן × שנה × חודש |
| יעדים | יעד שנתי ללקוח |
| פעילות | שיחות, פגישות, הצעות מחיר ותזכורות מעקב |

**מפתח חשוב:** אותו צמד לקוח–משלם יכול להופיע באותו חודש תחת שני סוכנים
שונים (10 מקרים כאלה בדוחות), ולכן הסוכן הוא חלק מזהות תנועת המכירה ולא רק
תכונה שלה.

**שלושה דברים שהדוח לא אומר במפורש, והמערכת מסווגת:**

| מה | למה זה משנה |
|---|---|
| **"לקוחות שונים"** (20289001) הוא סל מרוכז ולא חברה | המחזור שלו אמיתי ונספר בסיכומים, אבל אין למי להתקשר — ולכן הוא מוחרג מהדירוגים ומרשימות הטיפול, שם היה תופס מקום של לקוח אמיתי |
| **מספר שהוא קידומת של מספר אחר הוא חשבון אב**, והארוך ממנו אתר או חטיבה שלו | ארבעה זוגות כאלה, למשל קרגל משמר השרון מול משמר דוד. הדוח מציג אותם כשני לקוחות — וזה נכון — אבל הקשר מוצג בכרטיס |
| **12 גופים מופיעים רק כמשלמים** ולא כיעד משלוח | הם אינם לקוחות בתיק, ולכן אינם נספרים ככאלה |

קובץ הזינוך דחוס: סוכנים ולקוחות מופיעים פעם אחת, וכל תנועה מפנה אליהם לפי
מיקום ברשימה — 195KB לחמש שנים (6,947 תנועות, 291 גופים) במקום כחצי מגה.
לקובץ יש מספר גרסה: דוח חדש שנכנס למכשיר שכבר מחזיק נתונים מחליף את שורות
ה-ERP בלבד, ומשאיר עריכות ידניות, יעדים, פרופילים ורישומי פעילות כמו שהם.

## 4. המסכים

### בורר השנה
השנה והסוכן יושבים בסרגל מעל התוכן — לא בתפריט צד שנסגר — ונדבקים לראש המסך
בגלילה: מה שקובע כל מספר על המסך צריך להיות גלוי בזמן שקוראים אותו. חמש
שנים הן חמישה כפתורים, לא רשימה נפתחת שמסתירה את מה שיש בה.

### לוח בקרה
- מספר ראשי אחד — מכירות מתחילת השנה — עם השינוי מול התקופה המקבילה וגרף מצטבר.
- **פס השנים** מתחת למספר: חמישה עמודונים בגובה יחסי, הנבחרת מודגשת. הוא
  עונה על השאלה שמתעוררת מיד אחרי המספר — "וזה טוב?" — ומחליף שנה בנגיעה.
- **ההשוואה הוגנת:** נמדדים רק חודשים מלאים שיש בשתי השנים. המספר הגדול הוא
  מה שנמכר בפועל (ינואר–ספטמבר 2026), וההשוואה היא ינואר–אוגוסט מול
  ינואר–אוגוסט — כי חודש חצי מול חודש שלם אינו הפרש עסקי אלא תאריכי.
- **לשנה הראשונה אין מול מה להשוות**, והמסך אומר זאת במקום לצבוע כל לקוח
  כ"חדש" ואת כל המחזור כצמיחה.
- ארבעה מדדים: הפרש מול אשתקד, תחזית לסוף השנה לפי קצב נוכחי, ריכוז עשרת
  הגדולים, ולקוחות חדשים (או עמידה ביעד, כשהוגדרו יעדים).
- גרף עמודות חודשי, דירוג עשרת הגדולים ועשרת הצומחים.
- **דורש טיפול** ו**שקט על הקו** — הרשימות מוצגות במלואן, לא מקוצצות, עם
  מונה בכותרת וגלילה פנימית.
- משימות פתוחות, ממוינות לפי תאריך המעקב.

### מגמה
המסך שחמש השנים הצדיקו, ובו שלוש צורות שלכל אחת תפקיד משלה:

- **סך שנתי** — כמה. השנה הנבחרת מודגשת, השאר אפורות.
- **שנים-עשר חודשים מתגלגלים** — לאן, בלי שהעונתיות תצייר גלים מדומים. הקו
  נעצר בחודש המלא האחרון.
- **מפת חום של חודש מול שנה** — מתי בשנה. הסולם נמתח בין החודש הנמוך לגבוה
  ולא מאפס, אחרת כל החודשים היו נצבעים באותה מדרגה.

לצדם תמהיל הסוכנים לאורך השנים, ורשימות מי צמח ומי נשחק מאז השנה הראשונה —
בהשוואה על אותם חודשים בדיוק בשתי השנים.

### לקוחות
טבלה עם חיפוש, סינון מהיר (הכול / קנו השנה / דורש טיפול / שקטים / חדשים),
סינון לפי סטטוס ומיון לפי כל עמודה: מכירות השנה, תקופה מקבילה, אחוז שינוי,
מגמה חודשית, מסלול חמש-שנתי וחודש המכירה האחרון. המסלול הרב-שנתי עונה על
השאלה הראשונה שנשאלת מול טור שינוי אדום — האם זו מגמה או חודש חלש.

### כרטיס לקוח
נפתח בארבע לשוניות — סקירה, היסטוריה, פרטים ופעילות: מדדי השנה, גרף השוואה
חודשי, פירוט הלקוחות המשלמים, היסטוריה שנתית מלאה, יעד, פרטי קשר, הערות
ורישומי פעילות. לשונית ההיסטוריה פותחת בגרף חמש-שנתי, והשנה הנבחרת מודגשת בו.

### טבלת חודשים
הטבלה של הדוח, אבל כל תא ניתן לעריכה. התא מציג מספר מעוצב כשאינו בעריכה
ומחליף לערך הגולמי בכניסה אליו, כך שהטבלה נקראת כמו דוח ונערכת כמו גיליון
בלי לאבד אגורות. ניווט בחצים, Esc לביטול, ועמודת הלקוח נשארת גלויה בגלילה.

**בטלפון** ארבע-עשרה עמודות אינן נקראות ואינן נערכות, ולכן המסך הופך לרשימת
לקוחות: שורה עם הפס החודשי והסכום, ונגיעה פותחת גיליון של שנים-עשר שדות
גדולים שנשמר בבת אחת.

### משימות ופעילות
המסך נשאל שאלה אחת — מה צריך לעשות עכשיו — ולכן הוא ערימות לפי דחיפות ולא
רשימה אחת ממוינת: **באיחור, היום, השבוע, בהמשך, וללא תאריך מעקב**, כל אחת עם
המונה שלה. משימה בלי תאריך אינה נעלמת: היא ערימה בפני עצמה, כי בדיוק שם
נופלות ההבטחות.

הרישום הוא שורה אחת בראש המסך — מה צריך לעשות, לקוח, ותאריך בנגיעה ("היום",
"מחר", "בעוד שבוע") — ובטלפון כפתור צף שפותח גיליון. כל שורה נושאת סימון
ביצוע, דחייה ביום או בשבוע, ומחיקה.

### נתונים וקבצים
ייצוא וייבוא אקסל, גיבוי מלא של המערכת, ואיפוס לנתוני הדוחות המקוריים,
וטבלת ההצלבה מול הדוח המקורי.

### העוזר
פאנל צד במחשב, מסך מלא בטלפון. מנוע שאילתות דטרמיניסטי — לא מודל שפה ולא
קריאת רשת: הוא מזהה כוונה (סך מכירות, לקוח מסוים, חודש, סוכן, מי בסיכון,
מי שקט, מי חדש, מי צומח, מי גדול, תחזית) ובונה את התשובה מאותם חישובים
שמזינים את המסכים. מכאן שתי תכונות שחשובות יותר מכיסוי רחב: הוא לא ממציא
מספרים, וכשאין כיסוי לשאלה הוא אומר זאת במקום לנחש. כל שם לקוח בתשובה הוא
קישור לכרטיס.

זיהוי שם לקוח מתעלם ממילים שמופיעות גם בשאלות וגם בשמות חברות ("לקוחות",
"חברה", "ישראל"), אחרת "מי הלקוחות הגדולים?" נתפס כשאלה על החברה ששמה
"לקוחות שונים".

## 5. עיצוב וחוויית משתמש

- **טיפוגרפיה:** Heebo — נבנתה כבת-הזוג העברית של Roboto לממשקים. כל המספרים
  בספרות טבלאיות ובכיוון שמאל-לימין, כך שעמודות מתיישרות לפי הספרה וסימן
  המינוס לא קופץ לצד השני.
- **צבע:** בסיס ניטרלי קריר, צבע מותג אחד ושני צבעים סמנטיים בלבד. הכול
  ב-oklch, כך שהבהירות נשמרת בין הגוונים ובין מצב בהיר לכהה.
- **גרפים:** לכל גרף תפקיד אחד. עוצמה (מפת העונתיות) היא גוון אחד בחמש
  מדרגות מבהיר לכהה, וכל מדרגה קיבלה דיו משלה כדי שכל תא יעמוד בניגודיות
  של 4.5:1 ומעלה — לא רק רובם. השוואה לאשתקד היא הדגשה — צבע אחד מודגש מול
  אפור, ולא שני צבעי זהות. שינוי מול אשתקד הוא קוטביות — שני צבעים מנוגדים
  סביב אפס. פילוח לפי סוכן הוא זהות — שלושה גוונים קבועים שנבדקו להפרדה
  לעיוורי צבעים בשני המצבים. לכל גרף יש תצוגת טבלה מקבילה, כך שאף ערך אינו
  נגיש רק דרך ריחוף.
- **מצב כהה** מלא, כולל הגרפים — הם קוראים את הצבעים ממשתני ה-CSS.
- **סרגל צד** במחשב; בטלפון סרגל תחתון עם ארבעה יעדים ושטחי נגיעה של 55px.
  נקודת ההתראה מסמנת רק דברים שדורשים פעולה — מספר הלקוחות הפעילים אינו
  התראה, ונקודה אדומה לידו רק מלמדת להתעלם ממנה.
- **יום / לילה / אוטומטי.** ההעדפה נשמרת, ורק התוצאה נחתמת על ה-HTML, כך
  שיש נקודת אמת אחת לצבעים. רקע הלילה הוא ‎#12151b ולא שחור מוחלט: על שחור
  טהור טקסט לבן בוהק ומטשטש, והמשטחים מאבדים עומק.
- **בטלפון** הטבלאות נפרשות לכרטיסים, המגירות הופכות לגיליונות מסך מלא,
  וסדר המסך משתנה — קודם המספר ומי דורש טיפול, הגרפים אחריהם.
- **חיפוש מהיר** ב-Ctrl+K (או `/`) מכל מקום, עם ניווט בחצים.
- **ביטול** ב-Ctrl+Z לכל שינוי, וגם מתוך ההודעה שמופיעה אחרי הפעולה.
- אייקונים מצוירים כ-SVG בקו אחיד — לא אמוג'י.

## 6. ארכיטקטורה

```
PDF ×5 (ERP)  →  tools/parse_pdf.py  →  build_dataset.py  →  dataset.js
                                                                 ↓
                                                    הדפדפן (localStorage)
                                                          ↓
                                                    קובץ אקסל / גיבוי
```

HTML, CSS ו-JavaScript בלבד. בלי שלב בנייה, בלי תלויות בזמן טעינה: הגרפים
נבנים כ-SVG בקוד, וספריית האקסל נטענת רק כשמייצאים או מייבאים קובץ.

## 7. מה לא נכלל

- אין חיבור ישיר ל-ERP. עדכון תקופתי נעשה בהרצת הסקריפט על דוח חדש, או
  בייבוא אקסל.
- אין שרת, אין משתמשים ואין הרשאות. הנתונים חיים בדפדפן של המשתמש; שיתוף
  נעשה דרך קובץ.
- אין רווחיות או כמויות — הדוח מספק מחיר כולל בלבד.
